id: IE
title: Inbox Endpoint
description: >-
  The inbox endpoint is the primary entry point for actor-to-actor communication in Vultron.
  It receives ActivityStreams activities via HTTP POST, validates them, and queues them for
  asynchronous processing.
version: 1.0.0
scope:
- prototype
- production
tags:
- messaging
groups:
- id: IE-02
  title: Endpoint Configuration
  specs:
  - id: IE-02-001
    priority: MUST
    kind: protocol
    statement: The endpoint URL MUST be discoverable from actor profile
    rationale: Allow flexibility for different deployment contexts
  - id: IE-02-002
    priority: MUST
    kind: protocol
    statement: The endpoint MUST accept POST requests only
  - id: IE-02-003
    priority: MUST
    kind: protocol
    statement: The endpoint MUST reject non-POST requests with HTTP 405
    relationships:
    - rel_type: depends_on
      spec_id: HTTP-03-001
- id: IE-03
  title: Request Handling
  specs:
  - id: IE-03-001
    priority: MUST
    kind: protocol
    statement: The endpoint MUST validate Content-Type header
    relationships:
    - rel_type: depends_on
      spec_id: HTTP-01-001
  - id: IE-03-002
    priority: MUST
    kind: protocol
    statement: The endpoint MUST enforce a configurable maximum request body size.
    relationships:
    - rel_type: depends_on
      spec_id: HTTP-02-001
  - id: IE-03-003
    priority: MUST
    kind: protocol
    statement: Requests exceeding the maximum request body size MUST be rejected with HTTP 413.
    rationale: >-
      Rejecting oversized payloads with an explicit 413 prevents resource exhaustion
      and signals the limit violation to the sender.
    relationships:
    - rel_type: refines
      spec_id: IE-03-002
  - id: IE-03-004
    priority: MUST
    kind: protocol
    statement: >-
      The default maximum request body size MUST be documented in the deployment
      configuration reference.
    rationale: >-
      Operators need to know the default limit to tune it for their deployment
      environment.
    relationships:
    - rel_type: refines
      spec_id: IE-03-002
- id: IE-04
  title: Activity Validation
  specs:
  - id: IE-04-001
    priority: MUST
    kind: protocol
    statement: The endpoint MUST validate activity structure via schema validation
  - id: IE-04-002
    priority: MUST
    kind: protocol
    statement: The endpoint MUST return HTTP 422 for validation failures
    relationships:
    - rel_type: depends_on
      spec_id: HTTP-03-001
- id: IE-05
  title: Response Timing
  specs:
  - id: IE-05-001
    priority: MUST
    kind: protocol
    statement: The endpoint MUST return HTTP 202 within 100ms
    relationships:
    - rel_type: depends_on
      spec_id: HTTP-06-001
  - id: IE-05-002
    priority: MUST_NOT
    kind: protocol
    statement: The endpoint MUST NOT block on handler execution
- id: IE-06
  title: Asynchronous Processing
  specs:
  - id: IE-06-001
    priority: MUST
    kind: project
    statement: The endpoint MUST queue activities for background processing
  - id: IE-06-002
    priority: MUST
    kind: project
    statement: Background processing MUST be isolated from HTTP request/response cycle
- id: IE-07
  title: Error Responses
  specs:
  - id: IE-07-001
    priority: MUST
    kind: protocol
    statement: >-
      The endpoint MUST return HTTP 202 for successfully accepted activities,
      HTTP 400 for malformed requests or Activities whose addressing provably
      excludes the receiving actor (IE-11-001), HTTP 405 for non-POST methods,
      HTTP 413 for oversized request bodies, and HTTP 422 for activities that
      fail schema validation.
    relationships:
    - rel_type: depends_on
      spec_id: HTTP-03-001
- id: IE-08
  title: Response Format
  specs:
  - id: IE-08-001
    priority: MUST
    kind: protocol
    statement: 'Response body MUST be JSON with fields: `status`, `activity_id`, `message`'
    relationships:
    - rel_type: depends_on
      spec_id: EH-05-001
- id: IE-09
  title: Logging
  specs:
  - id: IE-09-001
    priority: MUST
    kind: protocol
    statement: The endpoint MUST log all requests at INFO level
    relationships:
    - rel_type: depends_on
      spec_id: SL-01-001
    - rel_type: depends_on
      spec_id: SL-02-001
    - rel_type: depends_on
      spec_id: SL-03-001
- id: IE-10
  title: Idempotency
  specs:
  - id: IE-10-001
    priority: SHOULD
    kind: protocol
    statement: The endpoint SHOULD implement early duplicate detection at HTTP layer
    relationships:
    - rel_type: depends_on
      spec_id: ID-05-002
- id: IE-11
  title: Activity Addressing
  specs:
  - id: IE-11-001
    priority: MUST
    kind: protocol
    statement: >-
      The inbox endpoint MUST refuse with HTTP 400 any Activity whose addressing
      fields (to, cc, bto, bcc) are all non-empty and none of them names the
      receiving actor. The refusal MUST be synchronous, before the HTTP 202 is
      returned, and MUST NOT write anything to the receiving actor's store.
    rationale: >-
      Per-actor DataLayer isolation (ADR-0070) makes each actor's store the
      authoritative answer to "whose replica is this?". That invariant is only
      trustworthy if the store's owner was the intended recipient of every
      Activity it processed. A synchronous 4xx is itself the signal to the sender,
      avoiding silent cross-actor data contamination.
    adr:
    - ADR-0068
  - id: IE-11-002
    priority: MUST
    kind: protocol
    statement: >-
      The inbox endpoint MUST accept Activities whose addressing fields
      (to, cc, bto, bcc) are all absent or unresolvable (Liberal Accept).
      An address is unresolvable if it cannot be confirmed to refer to a
      specific individual actor — for example, a collection URI such as
      ``{case_id}/participants``.
    rationale: >-
      Requiring explicit addressing would break flows where Activities are sent
      without formal addressing (e.g., internal relay, legacy senders). The
      narrowest thing that can be refused is provable exclusion; uncertainty
      defaults to acceptance. Fan-out deliveries using collection URIs (e.g.
      ``{case_id}/participants``) cannot be resolved to individual actors at
      inbox processing time and must therefore be accepted.
    adr:
    - ADR-0068
  - id: IE-11-003
    priority: MUST
    kind: protocol
    statement: >-
      The inbox endpoint MUST consider all four addressing fields — to, cc,
      bto, bcc — when determining whether the receiving actor is named as a
      recipient. A match on any one field is sufficient.
    rationale: >-
      Several protocol flows rely on cc-to-self loopback delivery. Checking
      only the to field would incorrectly refuse legitimately addressed Activities.
    adr:
    - ADR-0068
