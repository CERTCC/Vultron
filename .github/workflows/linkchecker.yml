name: Link Checker
on:
  pull_request:
    paths:
      - '**/*.md'
      - .github/workflows/linkchecker.yml
      - requirements.txt
  workflow_dispatch:

jobs:
  linkcheck:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.13'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip uv
          uv sync --dev 

      - name: Build Site
        run: |
          uv run --project=rc mkdocs build --verbose --clean --config-file mkdocs.yml

      - name: Check links
        run: |
          uv run linkchecker site/index.html
