"""
CLI driving adapter — stub.

Translates command-line invocations into core use-case calls.
Implements the CLI port of the hexagonal architecture.

Future implementation will use ``click`` (consistent with ``vultron/demo/cli.py``)
and delegate to the same use-case callables used by the HTTP inbox adapter.
"""
