---
source: IDEA-2838
timestamp: '2026-09-23T19:50:30.973521+00:00'
title: 'G10: Agent workflow tooling'
type: idea
---

Planning group **G10** of 19. Parent: #2828 — read its body for the planning protocol that applies to every group (umbrella-is-not-a-defect-claim, Tasks parent to the domain epic, nothing closes before it is in `origin/main`, lose nothing).

**Members**: #1984, #1907, #1857, #1464, #2145, #526, #2770, #2771
**Domain epic for Tasks**: #1190 (project infrastructure upgrades)
**Blocked by**: nothing.

## Why these are grouped

All eight concern the agent skill suite itself, and #1984 is the anchor: if `build` and
`bugfix` really are two flavors of one skill, then #1907 and #1857 fall out of the same
refactor rather than needing separate fixes.

## Members

- **#1984 — `build` and `bugfix` may be two flavors of the same skill; refactor
  potential.** The anchor. Settle this first; it determines whether the rest are
  independent fixes or fallout.
- **#1907 — the build workflow has no gate preventing work on already-implemented
  issues.**
- **#1857 — BW-01-003 / BW-02-002 learning-filename rule collides when one issue produces
  multiple learnings.** size:S.
- **#1464 — add a `lint-notes` skill for semantic `notes/` maintenance.**
- **#2145 — restructure the `pr-triage` skill to use an ultracode parallel workflow for
  phases 5–11.**
- **#526 — scheduled GitHub Action for automated monthly/quarterly project reports.**
- **#2770 — `plan-issue` SKILL.md: an agent-instruction annotation inside a `--body`
  heredoc is posted verbatim as issue body text.** Tier `Next`.
- **#2771 — `bugfix` REFERENCE.md: `PARENT_ARG` used unquoted, word-splits if
  `ISSUE_NUMBER` is empty.** Tier `Next`.

## Scope note — two of these need no planning

**#2770 and #2771 are one-line documentation/quoting bugs.** Do not run a design
conversation on them. Fix them directly as part of this session's PR, or file them as
immediate Tasks. They are listed here only so they are not lost.

## Desired output

- A verdict on #1984 with the refactor shape if yes.
- Task set parented to #1190, sequenced so the #1984 decision precedes anything that
  depends on it.
- `AGENTS.md` **Common Pitfalls** entries where a member describes a recurring agent
  failure (#1907 and #1857 both qualify).

## Constraints

- This group edits the skills that other agents run. Changes to `plan-issue`, `build`, or
  `bugfix` affect every future session — keep them small and verified.

## Acceptance criteria

- [ ] AC-1: A verdict on #1984 — one skill or two — with the refactor shape if merging.
- [ ] AC-2: #2770 and #2771 fixed directly or filed as immediate Tasks, with no design
      conversation spent on them.
- [ ] AC-3: `AGENTS.md` Common Pitfalls entries added for the recurring failures in
      #1907 and #1857.
- [ ] AC-4: Tasks parented to #1190 and sequenced behind the #1984 decision where they
      depend on it.

---

**Processed**: 2026-09-23 — implementation tracked in #3582, #3586, #3587, #3588; prose-AC gate folded into existing #3036 (blocked by #3582).
Docs PR: <https://github.com/CERTCC/Vultron/pull/3581>.
Spec: `specs/build-workflow.yaml` (BW-01-003).
Notes: `notes/agentic-workflow.md`, `notes/git-workflow-pitfalls.md`.

Verdicts: #1984 — two skills, shared `shared/` fragments (#3582). #1907 — gate exists, prose-AC remainder in #3036. #1857, #2770, #2771 — fixed in the docs PR. #1464 — re-scoped to mechanical checks consumed by `learn` (#3586). #2145 — #3587. #526 — #3588, re-scoped from Copilot to Claude Code, needs maintainer approval (CI).
