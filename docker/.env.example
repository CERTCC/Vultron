# Environment Variables for Docker Setup
# Copy this file to .env and modify the values as needed
# before using it with Docker Compose.

# Project name used for Docker Compose resource naming
# This is used to prefix container, network, and volume names
PROJECT_NAME=vultron

# Alternative Docker Compose project name variable
# Some docker-compose versions use this instead
COMPOSE_PROJECT_NAME=vultron
